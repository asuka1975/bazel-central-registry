--- a/bazel/build_defs.bzl
+++ b/bazel/build_defs.bzl
@@ -39,8 +39,6 @@ _DEFAULT_CPPOPTS.extend([
 ])
 _DEFAULT_COPTS.extend([
     "-std=c99",
-    "-pedantic",
-    "-Werror=pedantic",
     "-Wall",
     "-Wstrict-prototypes",
     # GCC (at least) emits spurious warnings for this that cannot be fixed

